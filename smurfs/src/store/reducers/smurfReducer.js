const intialState = {
    smurfs: [],
    loading: false,
    error: ''
}

export const smurfReducer = (state=intialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}